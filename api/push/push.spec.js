/* eslint prefer-arrow-callback:0, func-names:0 */
require('chai').should();

const app = require('../../app');

require('chai').should();

describe('/follow api', function () {
  before(function (done) {
    c1=circleDAO.createCircle().id;
    m1=mailboxDAO.createMailbox().id;
    m2=mailboxDAO.createMailbox().id;
    m3=mailboxDAO.createMailbox().id;
    done();
  });

});